# Evergreen Quote — AI-Assisted Feature Work (Solution)

The completed reference. `evergreen-features.js` contains the verified,
tested implementations, and `prompts.solution.md` shows a strong prompt.

```bash
node --test   # all tests pass
```

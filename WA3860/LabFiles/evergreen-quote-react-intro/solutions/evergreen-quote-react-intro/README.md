# Evergreen Quote — Intro to React (Solution)

The completed reference app. The three components in `src/components/` contain
the finished `useState`, props, and list-rendering code from the lab guide.

```bash
npm install
npm run dev
```

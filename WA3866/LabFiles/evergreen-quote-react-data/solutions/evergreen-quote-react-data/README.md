# Evergreen Quote — Beyond the Basics (Solution)

The completed reference app. `src/components/RecentQuotes.tsx` fetches the
recent quotes with `useEffect`, handles loading and error states, and cancels
the request on unmount with an `AbortController`.

```bash
npm install
npm run dev
```

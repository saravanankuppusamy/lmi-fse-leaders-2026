# Evergreen Quote — TypeScript Build (Solution)

The completed reference project. `src/types.ts` and `src/premium.ts` contain the
finished type definitions and annotations described in the lab guide.

```bash
npm install
npm run dev      # run the app
npm run build    # type-check + build — passes with no errors
```
